polyfill

adapto una función que un buscador puede que no tenga, en este caso reemplazo el "*" de multiplicar
pr un método:

Number.prototype.multiplicar = function (num) {
  let accu = 0
  
  for(let i = 0; i < num; i++) {
    accu += this
  }
  
  return accu
}

const myNum = 5
const myNum2 = 10

const result = myNum.multiplicar(5)
const result2 = myNum2.multiplicar(20)

console.log(result)
console.log(result2)

==========

operador ternario

const condition = true;
let reult = null

if (condition) {
  result = "correct"; 
}else{
  result = "incorrect";
}
console.log(result);

console.log(`${condition ? 'correct' : 'incorrect'}`);


===============

const vehiculo ={
  marca: 'bmw',
  modelo: '325',
  year: 2010
}

const vehiculo2 = {
  ...vehiculo
}

//vehiculo2 tendrá en sus campos los mismos valores que vehiculo, pero estos se pueden editar